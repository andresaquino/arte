# .bash_profile
# vim: set ts=2 sw=2 sts=2 et si ai syn=sh:

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
  . ~/.bashrc
fi

# User specific environment and startup programs
PATH=${PATH}:${HOME}/bin
export PATH

